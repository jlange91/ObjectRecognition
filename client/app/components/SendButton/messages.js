/*
 * SendButton Messages
 *
 * This contains all the text for the SendButton component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.SendButton.header',
    defaultMessage: 'Send',
  },
});
